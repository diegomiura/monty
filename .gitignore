__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/
.env
data/raw/*.csv
data/raw/*.json
# Club sources (football-data.co.uk) are free to download and analyse but
# carry no redistribution grant — see reports/CLUB_DATA_AUDIT.md. Never
# commit them; the pipeline re-downloads and caches at runtime.
data/raw/clubs/
data/interim/
data/processed/
models/*.joblib
reports/figures/*.png
# ...except the two summary figures embedded in the README
!reports/figures/wc_all_frozen_reliability.png
!reports/figures/importance_gb_permutation.png
